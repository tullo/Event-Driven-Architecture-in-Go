
## Run monolith

Start the monolith version of the application:

```sh
docker compose --profile monolith up --build
```

## Run microservices

Start the microservices version of the application:

```sh
docker compose --profile microservices up --build
```
